#ifndef _800627D8_H_
#define _800627D8_H_

#include <types.h>

u32 getLuigiAppearPoint(); //fn_800627D8() I assume this is probably the one set from LM's custom events but not sure.

static u32 sGlobalLuigiAppearPoint; //lbl_804D872C

#endif
